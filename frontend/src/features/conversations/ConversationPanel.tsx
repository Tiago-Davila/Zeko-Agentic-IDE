import { useState } from 'react';
export function ConversationPanel({ projectId }: { readonly projectId: string | null }) { const [message, setMessage] = useState(''); return <section aria-label="Conversación"><h3>Conversación con PM o agente</h3><p>Podés instruir directamente al PM o a un agente; el origen queda trazado.</p>{projectId === null ? <p>Seleccioná un proyecto para conversar.</p> : <form onSubmit={(event) => { event.preventDefault(); setMessage(''); }}><label>Instrucción<input aria-label="Instrucción" value={message} onChange={(event) => setMessage(event.target.value)} /></label><button type="submit">Enviar instrucción</button></form>}</section>; }
